version: 2

sources:
  - name: raw
    database: "{{ var('raw_database', 'RAW_DEV') }}"
    schema: 'finance'
    description: "Raw tables for finance data"
    config:
      freshness:
        warn_after:  {count: 48, period: hour}
        error_after: {count: 72, period: hour}
      loaded_at_field: _loaded_at

    tables:
      - name: fx_rates
        description: "Raw fx_rates records"
