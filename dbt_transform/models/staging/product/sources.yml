version: 2

sources:
  - name: raw
    database: "{{ var('raw_database', 'RAW_DEV') }}"
    schema: product
    description: "Raw tables for product data"
    config:
      freshness:
        warn_after:  {count: 48, period: hour}
        error_after: {count: 72, period: hour}
      loaded_at_field: _loaded_at

    tables:
      - name: order_items
        description: "Raw order items records"
